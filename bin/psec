#!/usr/bin/env python

# Wrapper with short name to provide a 'script' for pip installation.
# This is required by 'pipsi', which doesn't use console_scripts
# entry points.

import sys

from python_secrets.main import main

if __name__ == "__main__":
    sys.exit(main())

============
Installation
============

At the command line::

    $ easy_install python_secrets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python_secrets
    $ pip install python_secrets
